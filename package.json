{
  "name": "field-crm",
  "private": true,
  "description": "Test harness only. The app itself has no build step and no runtime dependencies.",
  "type": "module",
  "scripts": {
    "test": "node tests/run.mjs"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^30.0.1"
  }
}
